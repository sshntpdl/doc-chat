// FILE: /apps/web/postcss.config.mjs
// Tailwind CSS v3 PostCSS config.
// tailwindcss + autoprefixer is the standard v3 setup for Next.js.

const config = {
  plugins: {
    tailwindcss:  {},
    autoprefixer: {},
  },
};

export default config;
